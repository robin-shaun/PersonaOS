# PersonaOS 路线图

PersonaOS 的目标是逐步构建一个有证据、可审核、可修正的个人智能体，而不是宣称
上传意识、复活逝者或完整复制人格。路线图按可运行产品闭环推进；后续方向不是
发布日期承诺，也不会为了“看起来完整”提前建立空模块。

## 当前：第一阶段，数字员工与人物记忆 MVP

0.12.0 已形成以下可本地演示的闭环：

- **M1 · 资料到候选**：人物档案、加密原文、稳定分块、来源定位、待审核候选；
- **M2 · 记忆到回答**：人工确认、embedding 空间、混合检索、可解析 citation；
- **M3 · 隐私生命周期**：不可变版本、关系、敏感等级、导出、级联删除与墓碑；
- **M4 · 产品入口**：React 工作台、API/Worker、PostgreSQL/pgvector Compose、
  虚构 Demo 和 Web-origin smoke；
- **M5 · 开源发布基线**：Apache-2.0、社区与安全政策、API/Skill 文档、架构图、
  双依赖锁、容器 digest、最小权限 CI、发布说明和自动 release gate。
- **M6 · 可信本地账户**：Argon2id、可撤销会话、CSRF/Origin、服务端 actor、
  全领域 owner 过滤、PostgreSQL FORCE RLS、近期再认证和显式 legacy 迁移。

这仍是 Alpha。离线 embedding 是特征哈希，人物回答默认只复述证据；没有可信
公网身份平台、第三方 Skill 沙箱、云/本地通用模型适配器或数字人渲染。

## 唯一下一里程碑：M7 记忆真实性分类与人物时间线

在可信账户边界上，下一步先让人物记忆的真实性类型与事件时间成为可查询、
可修正的一等数据，而不是开始风格模仿。M7 只交付一个纵向闭环：

1. 统一区分用户明确提供、资料可验证、系统总结、模型推断和用户设定；
2. 每个结论保留来源、定位、事件时间、记录时间、版本、置信度和确认状态；
3. 从确认记忆生成可筛选人物时间线，不修改原始资料或旧版本；
4. 同一主题/时间的支持与冲突可以被检测并展示给用户；
5. 问答明确显示使用了哪种真实性类型，推断不能伪装成历史事实；
6. 用户可以修正事件时间或分类，并保留变更前后的证据链；
7. API、Web、导出、审计与测试形成完整纵向闭环。

验收要求包括：五类真实性在存储与 UI 中不可混淆；时间线项目可回到原始定位；
冲突不会自动覆盖旧记忆；修改产生新版本；回答和导出保留分类与不确定性。在这些
测试通过前，不开始更大范围的语言风格或价值判断建模。

## 第二阶段方向：有证据的数字分身

M7 稳定后才依次评估以下产品能力，每次仍需单独收敛为一个里程碑：

- 情景、语义、程序性、偏好、关系与反思记忆的独立写入规则；
- 记忆支持/冲突检测、人工合并和版本比较；
- 表达风格样本、适用场景、置信度与避免模仿的敏感边界；
- 可替换的 OpenAI-compatible、Ollama/vLLM 模型与 embedding 适配器；
- 关系检索和时间检索；只有真实查询证明必要时才引入图数据库；
- 第三方 Skill 的签名、权限差异审核、隔离、资源配额、启停、升级与回滚；
- OpenTelemetry 运行观测，同时保持领域审计为独立事实记录。

阶段退出条件不是“回答更像”，而是每种个人化主张都能显示证据类别、来源、
版本、置信度和冲突；用户可以纠正，系统不会把推断改写成历史。

## 第三阶段扩展点：连续数字生命研究

前两阶段成熟后，才探索长时间运行、自主整理新经历、缓慢偏好演化、多模态记忆、
主动协助、多智能体协作、数字遗产与授权。ASR、TTS、口型/表情和视频/三维渲染
保持独立适配器；形象初始化与运行推理是不同阶段，运行时仍可能持续消耗 GPU。

任何“继承”“逝者分身”或代理现实人物的能力，都必须先具备可撤销授权、资料
主体/第三方权利处理、停用与销毁、能力声明、滥用响应和身份连续性边界。系统
始终是基于资料的模拟，不获得现实人物的法律身份、同意权或道德权威。

## 取舍触发条件

- 当模块需要独立扩缩容、故障域或数据权限时，才从模块化单体拆分服务；
- 当任务需要跨天等待、复杂补偿和高可用恢复时，才引入 Temporal 等工作流引擎；
- 当关系遍历成为有基准证明的主要瓶颈时，才引入图数据库；
- 当通用 Skill 安装具备签名、锁定、沙箱和权限审核时，才开放社区市场；
- 当认证、限流、秘密管理、备份恢复和渗透测试完成时，才讨论非本机部署基线。
