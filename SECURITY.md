# PersonaOS 安全策略

PersonaOS 会处理人物资料、关系、偏好和行为证据。一个普通功能缺陷也可能成为
隐私事件，请优先保护受影响用户，再讨论公开披露。

## 支持范围

| 版本 | 安全更新 |
| --- | --- |
| `0.11.x` | 支持 |
| `0.10.x` 及更早版本 | 不支持，请先升级并核对迁移与备份 |
| `main` | 开发分支，不承诺稳定，但接受漏洞报告 |

项目处于 Alpha 阶段，尚未完成独立安全审计。当前本地单所有者模式没有登录、
会话或远程多租户隔离，只允许绑定本机回环地址。不要把 API 或 Web 反向代理到
公网或不可信局域网。

## 私密报告漏洞

请使用仓库的
[GitHub Security Advisory](https://github.com/robin-shaun/PersonaOS/security/advisories/new)
提交私密报告。不要在公开 Issue、Discussion、PR、日志或演示数据中披露：

- 可利用步骤、未修复漏洞或个人身份信息；
- 人物资料、记忆正文、引用摘录、数据库或 Blob；
- API Key、GitHub token、App 私钥、Blob 密钥或部署地址。

报告中请提供受影响版本、最小复现、影响范围、攻击前提和可行缓解措施。请使用
完全虚构的数据；如果复现必须接触真实资料，先只描述数据类型并等待维护者协调
安全传输方式。

维护者目标是在五个工作日内确认收到报告，在十个工作日内给出初步影响判断。
这不是 SLA；复杂问题会持续通过 Advisory 更新进度。修复可用后，维护者会与
报告者协调公告时间、CVE 需求和致谢方式。未经协调，请不要在用户有合理升级
时间前公开利用细节。

## 安全边界

0.11.x 已强制以下基线：

- Compose 只把 API 和 Web 映射到 `127.0.0.1`；
- 原始 Blob 使用 AES-256-GCM，每次读取同时验证认证标签和内容哈希；
- 人物 owner 来自服务端配置，不信任人物请求中的调用方 ID；
- 记忆候选必须人工确认，只有当前 confirmed 版本能进入检索；
- 检索先执行 owner、persona、状态、可见性、敏感等级和模型边界过滤；
- 外部模型只能接收 public 记忆摘要和 citation ID，不能接收原始证据；
- 删除清理应用可达的 Blob、chunk、向量、引用和派生回答，并写无正文墓碑；
- GitHub 适配器只实现读取，GitHub App token 不持久化；
- Skill 执行检查岗位权限、工具和超时，但仅适用于仓库内受信代码；
- 容器基础镜像固定 digest，Python/npm 依赖锁定，CI Action 固定 commit SHA。

这些控制不代表生产认证。尤其要注意：

- chunk、候选、确认记忆和引用摘录仍是数据库可读字段，需要磁盘/卷加密；
- 自动生成的本地 Blob key 与数据位于同一卷，抵御不了主机被完全攻破；
- 应用层删除不等于 SSD、对象存储历史版本或备份介质的物理净化；
- SQLite 模式和 Compose 默认凭据只面向单机开发；
- 没有速率限制、账户恢复、MFA、行级安全策略或跨租户渗透测试；
- 第三方 Skill 安装和不可信代码隔离尚未实现；
- 外部模型、TTS、ASR 或数字人服务一旦启用，会形成新的数据处理方边界。

完整威胁边界和删除语义见 [架构文档](docs/architecture.md) 与
[ADR 0001](docs/adr/0001-memory-privacy-lifecycle.md)。

## 部署者责任

部署者应使用独立强密码、限制文件权限、加密磁盘和备份、把密钥放在数据卷之外
的秘密管理系统，并定期验证恢复与删除流程。接入外部服务前，要确认数据保留、
训练使用、地域、分包商和删除政策，并取得资料主体及第三方的必要授权。

升级前备份数据库、加密 Blob 和密钥；三者缺一都无法形成可恢复备份。安全事件
发生后不要删除日志或立即重建卷，应先隔离服务、轮换密钥、保存不含人物正文的
取证材料，再根据适用法律与授权范围通知受影响者。
